spring:
    messages:
        basename:
            ValidationMessages
    profiles:
        group:
            local:
                - api-repo-local
                - email-local
                - storage-local
            prd:
                - api-repo-prd
                - email-prd
                - storage-prd

log:
    file:
        path: ${LOGS_ABSOLUTE_PATH:./var/log/api}
